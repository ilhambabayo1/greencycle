# Copy your trained model here (produced by Greencycle1.ipynb via `model.save('my_model.keras')`).
# Optionally also save class_indices.json next to it to guarantee correct class ordering.
# Files in this folder are excluded from the Docker image (.dockerignore) and mounted as a volume instead.
